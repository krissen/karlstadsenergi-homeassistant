# Test dependencies for karlstadsenergi-ha
#
# Pinned exact for reproducible CI: a floating "latest" HA (or ruff/pytest)
# release periodically broke `main` with no change on our side. Dependabot
# proposes monthly bumps (see .github/dependabot.yml) so a breaking release
# surfaces as a red check on the bump PR instead of a red main.
# Dependabot only bumps `==` pins, so keep these exact.
#
# homeassistant is required to import the integration modules (CI/test only --
# not a runtime dependency; the manifest carries no HA version constraint).
homeassistant==2026.7.1
pytest==9.1.1
pytest-asyncio==1.4.0
